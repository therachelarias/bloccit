<h1><%= @question.title %></h1>

<%= link_to "Edit", edit_question_path(@question), class: 'btn btn-success' %>

<%= link_to "Delete", @question, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete your question?'} %>

<p><%= @question.body %></p>